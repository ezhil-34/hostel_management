# ---------------------------------------------------------------------------
# Root environment — read by docker compose. Copy to .env:
#   cp .env.example .env      (Windows PowerShell: copy .env.example .env)
#
# The defaults below are fine for local development. Change the secrets and
# the database password before running docker-compose.prod.yml anywhere real.
# ---------------------------------------------------------------------------

# --- PostgreSQL ---
# One server, one database per service. hostel_db belongs to the core API,
# maintenance_db to the maintenance service — neither can join across the two.
POSTGRES_USER=hostel
POSTGRES_PASSWORD=hostel
POSTGRES_DB=hostel_db
MAINTENANCE_DB=maintenance_db
POSTGRES_PORT=5432

# --- Ports on your machine ---
BACKEND_PORT=5000
MAINTENANCE_PORT=5100
FRONTEND_PORT=5173
ADMINER_PORT=8080
HTTP_PORT=80

# How long the maintenance service waits on the core API when snapshotting a
# reporter's details. Past this it logs the request without them.
CORE_API_TIMEOUT_MS=3000

# --- Auth ---
# Generate fresh values with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_ACCESS_SECRET=dev-access-secret-change-me
JWT_REFRESH_SECRET=dev-refresh-secret-change-me
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
BCRYPT_ROUNDS=12

# --- CORS / frontend ---
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
VITE_API_URL=/api

# Origin embedded in outpass QR codes. To scan a pass with a real phone, set
# this to your machine's LAN address, e.g. http://192.168.1.20:5173 — a QR
# containing "localhost" points the phone at itself. Restart the backend after
# changing it: docker compose up -d
APP_PUBLIC_URL=http://localhost:5173

# --- Seed data ---
SEED_PASSWORD=Password123
